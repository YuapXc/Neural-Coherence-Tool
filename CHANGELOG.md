# Changelog

## 1.3.2 - 2026-08-17

- 适配「同调计划」2.0.3 在“同调网络”标题后新增的好友总数，面板会将标题与数量整体作为左侧边界，避免覆盖括号统计。
- 标题间空间不足时自动使用 92–111 dp 的紧凑面板，不再因达不到旧版 112 dp 最小宽度而直接隐藏。
- 将普通互动随机间隔由 1.5–3 秒收敛为 0.5–1.5 秒；保留首次缓冲、服务端限流等待和单次重试保护。

## 1.3.1 - 2026-07-23

- 将批量互动的随机请求间隔由 0.5–1 秒调整为 1.5–3 秒，并在扫描结束后增加 3 秒缓冲。
- 服务端明确返回限流提示或 HTTP 429 时，等待 20 秒并重试本次请求一次；重试成功后继续处理剩余好友，其他业务失败、网络超时或重试仍受限时安全停止。
- 等待期间允许快速响应手动停止，并在面板和实时通知中显示限流等待及重试状态。
- 已在 Root + LSPosed API 102 环境完成 548 位好友的整轮互动验证，共成功处理 548/548，耗时约 20 分钟。

## 1.3.0 - 2026-07-20

- 将模块主入口、通知链路、广播组件和设置页全部迁移到 Kotlin，为后续引入 Compose 与 MIUIX 页面做好工程准备。
- 将好友扫描、互动请求、请求签名和错误映射拆分为独立的 `InteractionEngine`，页面识别与面板布局继续由可测试的纯逻辑组件负责。
- 增加统一任务状态机，确保只读扫描与批量互动不会重叠或相互排队；扫描期间会禁用互动入口。
- 修复 Flutter 首次启用语义输出时可能尚未就绪的问题：系统无障碍关闭时允许有限次数重试，避免冷启动偶发不显示面板。
- 将十分钟重复执行保护改为仅在批量互动完整成功后记录；网络失败、手动停止或接口错误不再产生无效冷却。
- 增加任务互斥、页面语义判定和面板布局单元测试，并保留 Kotlin 标准库以优先保证不同注入环境下的运行稳定性。
- 已在 Root + LSPosed API 102 与无 Root + NPatch API 102 测试版环境验证模块加载、无障碍开启/关闭、主页面板和前后台恢复。

## 1.2.1-fix - 2026-07-19

- 修复系统无障碍服务关闭时 Flutter 不生成语义节点，导致无 Root 设备的控制面板不显示。
- 在宿主前台按需开启 Flutter 内部语义输出，退出前台时恢复原状态，不修改系统无障碍设置。
- 修复宿主退到后台再返回后，旧语义缓存可能阻止面板重新显示的问题。
- 已在无 Root、NPatch 本地模式且系统无障碍关闭的真机环境验证首次进入及前后台恢复。
- 强烈建议无 Root 用户更新，并明确要求使用支持 libxposed API 102 的 NPatch 测试版。

## 1.2.1 - 2026-07-19

- 控制面板改为读取 Flutter 语义节点的实际几何位置，不再依赖固定顶部和右侧偏移。
- 根据「同调网络」标题与「设置特别通讯」完整入口之间的可用空间自动调整面板位置和宽度。
- 将「设置特别通讯」左侧五角星纳入右侧边界，避免面板遮挡图标或标题。
- 语义坐标短暂缺失时保留最近一次有效位置，持续不可用或空间不足时安全隐藏面板。
- 完成 NPatch 无 Root 环境下的 API 102 模块加载、扫描、`ping`、`pong`、进度通知和完成通知验证。
- 增加置顶的无 Root 使用入口，以及包含下载、Stealth 打包、模块作用域、登录限制和问题排查的图文教程。
- 明确重新打包会改变宿主签名、需要先卸载官方版本，并建议使用手机号登录。

## 1.2.0 - 2026-07-19

- 为扫描和批量互动增加 Android 原生实时进度通知。
- 在 Android 16 上使用 `Notification.ProgressStyle` 与提升式实时更新，不支持时自动降级。
- 支持从批量互动通知手动停止任务；只读扫描通知不显示无效的停止操作。
- 任务结束时先在实时更新中显示“已完成”和聚合结果 1 分钟，再转换为可清除的详细完成通知。
- 通知仅包含聚合进度，不展示好友或账号标识。
- 增加通知权限、提升式实时更新和 10 秒通知链路自检页面。
- 补充 ColorOS 自启动及第三方冻结工具白名单说明。
- 已知限制：实时通知提升样式主要在 Android 16 / ColorOS 16 验证，HyperOS 等定制系统可能存在显示细节差异。

## 1.1.0

- 改用 Flutter 语义树识别真实页面，仅在「同调网络」主页显示控制面板。
- 在好友详情、特别通讯、同调记录、状态页及其子页面中自动隐藏面板。
- 修复慢响应、连续点击、滑动和系统手势可能导致的面板误显示。
- 将稳定页面检测间隔调整为 1500 毫秒，并保留点击后的快速复查。
- 缓存 Flutter 语义字段、合并连续检测并减少临时对象分配。
- Activity 进入后台后停止页面检测，恢复时重新同步页面状态。
- 修复 Android 10 至 Android 12 上读取网络响应的 API 兼容问题。

## 1.0.0

- 首次公开发布。
- 支持只读好友状态扫描、顺序批量互动、随机请求间隔和手动停止。
- 提供重复执行保护、网络状态提示、接口结构校验和敏感日志保护。
